name: grit
version: "0.1"
author: Adi <apps@adizan.ga>
about: git in rust
args:
    - verbose:
        short: v
        multiple: true
        help: Sets the level of verbosity, not implemented yet tbh
subcommands:
    - init:
        about: Initialize empty git repository
    - find:
        about: Find git repo in this dir or one of it's parent dirs
    - test:
        about: For testing, functionality can be arbitrary
